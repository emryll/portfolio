#include <windows.h>
#include <stdio.h>

PVOID getFuncAddress(PVOID moduleBase, )

BOOL isHooked(int pid) {
    //* compare IAT and EAT addresses
    //* 

    return FALSE;
}

int main() {
    //* parse cmdline arguments and flags
    // -s for silent
    // -dll=[dll] to specify only certain dll to check, null for the main module
}